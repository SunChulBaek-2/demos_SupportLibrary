language: android

android:
  components:
    - android-22
    - build-tools-22.0.1

notifications:
  email:
    recipients:
      - x1210x@gmail.com
    on_success: always
    on_failure: always

before_install:
  - chmod +x gradlew
  - android update sdk --filter extra-android-support --no-ui --force
  - android update sdk --filter extra-android-m2repository --no-ui --force

script:
  - ./gradlew clean assemble